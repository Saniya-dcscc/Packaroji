<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ url_for('home', _external=True) }}</loc><priority>1.0</priority></url>
  <url><loc>{{ url_for('products', _external=True) }}</loc><priority>0.95</priority></url>
  {% for category in product_categories %}
  <url><loc>{{ url_for('product_category_page', category_slug=category.name|lower|replace(' & ', '-')|replace(' ', '-'), _external=True) }}</loc><priority>0.85</priority></url>
  {% endfor %}
  {% for product in products %}
  <url><loc>{{ url_for('product_detail', slug=product.slug, _external=True) }}</loc><priority>0.8</priority></url>
  {% endfor %}
  <url><loc>{{ url_for('order_page', _external=True) }}</loc><priority>0.9</priority></url>
</urlset>
