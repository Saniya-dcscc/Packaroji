services:
  - type: web
    name: packaroji
    runtime: python
    buildCommand: cd Packaroji && pip install -r requirements.txt
    startCommand: cd Packaroji && gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.11"
